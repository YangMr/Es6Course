数值的扩展:
    二进制和八进制表示法
    Number.isFinite(), Number.isNaN()
    Number.parseInt(), Number.parseFloat()
    Number.isInteger()
    Number.EPSILON
    安全整数和 Number.isSafeInteger()
    Math 对象的扩展
    指数运算符


    Number.isFinite();
    Number.parseInt();
    Number.parseFloat();
    Number.isNaN();
    Number.isInteger();
    Math.sign();
